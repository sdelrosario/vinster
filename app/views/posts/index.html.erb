
	<% @posts.each do |post| %>
 		
		<div class= "col-xs-10 col-xs-offset-1 col-sm-4 col-sm-offset-1 genie-box text-center">
 			<h1>
 				<%= post.title %>
 			</h1>
 			<%= image_tag post.image %>
 			<%= post.description %>
 		</div>

	<% end %>
